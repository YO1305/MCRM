/**
 * Old stage/threshold activity (Новый / Критический / Заморожен) is retired.
 * Monthly Groq labels live in activityLabel / activeDaysThisMonth.
 */
export function LeadActivityScanner() {
  return null
}
